import 'package:flutter/material.dart';

class HadesTap extends StatelessWidget {
  const HadesTap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(color: Colors.purple,);
  }
}